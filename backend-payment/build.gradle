apply from: "$rootDir/env-backend.gradle"

dependencies {
    compile project(":backend-shared-domain")
    compile project(":backend-shared-infrastructure")
    compile project(":backend-shared-application")

    testCompile libraries.test.mockito
}

bootRepackage.enabled = false
